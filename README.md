## Tutor class pages

- `general_class.html`: Multi-teacher version of `math.html` (each teacher has their own rows + defaults; saved to browser `localStorage`).
- `general_class_readonly.html`: Read-only view for `general_class.html`.
- `general_class_legacy.html`: Previous experimental version (kept for reference).
